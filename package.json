{
  "name": "angular-mf-travel",
  "private": true,
  "scripts": {
    "check": "cd app && bun run check",
    "dev": "cd app && bun run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
